---
"@philogag/pi-tool-presistant-bash": patch
---

Initial release: persistent bash session tools (create/exec/list/destroy) plus
container sessions (presistant-bash-create-container with docker/podman
run -d + exec -i)
